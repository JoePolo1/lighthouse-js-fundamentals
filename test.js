const whichSchool  = 12; //sets the variable whichSchool based on the age function
const school1 = "Elementary School"; // The next 3 lines define the different school options as variables
const school2 = "Secondary School";
const school3 = "Lighthouse Labs";

  if (whichSchool > 13) {
    console.log("Since the student is " + whichSchool + " years old, they should attend " + school1 + ".")
  }
